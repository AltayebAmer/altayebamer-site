# النشر | Deployment — AltayebAmer.com

موقع HTML خالص بلا خطوة بناء. الاستضافة: **Cloudflare Pages**، المستودع: **GitHub**.
Pure static HTML, no build step. Hosted on **Cloudflare Pages**, repo on **GitHub**.

---

## 1. قبل النشر — قائمة تحقق | Pre-launch checklist

### جاهز الآن | Done and ready
- [x] أيقونات كاملة + `site.webmanifest` + صورة مشاركة ١٢٠٠×٦٣٠
- [x] SEO على ٣٠ صفحة: canonical · Open Graph · Twitter · وصف لكل صفحة
- [x] بيانات منظمة JSON-LD (Person + WebSite)
- [x] `sitemap.xml` (٣٠ رابطاً، شامل قسم البيان الفني) و`robots.txt`
- [x] صفحة ٤٠٤ ثنائية اللغة مع زر لغة
- [x] ترويسات أمان: HSTS · nosniff · X-Frame · Referrer · Permissions
- [x] وصولية: تقليل الحركة · مؤشر طبيعي على اللمس · إبراز التركيز
- [x] حفظ اللغة عبر الصفحات ودعم `?lang=ar` — على ٣١ صفحة
- [x] honeypot وتحقق البريد في نموذجَي التواصل والطلب
- [x] لوحة التحكم: كلمة مرور مُشفّرة · قفل جلسة · إصلاح تعطّل الدخول · إيقاف أداة حذف الروابط
- [x] مانيفستات الصور أُعيد بناؤها: PIM ١٢٦/١٢٦ · GIM ٣٤٣/٣٤٣ (كانت ٥٧ و٣٤٣ مكسورة)
- [x] PIM صار يغطي قسم البيان الفني والمعرض العام لأول مرة
- [x] محرر الملفات: ٣٥ ملفاً بدل ٩

### يحتاج منك | Needs you
- [ ] **الصور الحقيقية** — ٤٧٠ خانة في `images/`، ٢٩ جاهزة. ضع الصور ثم `python3 tools/apply-images.py`
- [ ] **ربط النموذجين** — `FORMSPREE_ID` في `contact/` و`commission/`
- [ ] **الروابط الاجتماعية** في JSON-LD داخل `index.html`
- [ ] **مراجعة طول المقالات** (١١٢ مقالاً بمتوسط ٨٥ كلمة)

---

## 2. خطوات النشر | Deployment steps

1. ارفع كل الملفات (لا ملف ZIP) إلى مستودع GitHub.
   Push all files (not a ZIP) to a GitHub repo.

2. Cloudflare Pages → **Connect to Git** → اختر المستودع.

3. الإعدادات | Settings:
   - **Build command:** اتركه فارغاً | leave empty
   - **Output directory:** `/`

4. اربط النطاق `altayebamer.com` من لوحة Pages، وفعّل **Always Use HTTPS**.
   Attach the domain from the Pages panel and enable **Always Use HTTPS**.

5. بعد أول نشر ناجح | After the first successful deploy:
   - أرسل `https://altayebamer.com/sitemap.xml` إلى Google Search Console.
   - اختبر بطاقة المشاركة على WhatsApp / X / LinkedIn.
   - تأكد أن `https://altayebamer.com/altayeb-cms-v5.html` تعمل لك، ولا تظهر في نتائج البحث.

---

## 3. ربط نموذج التواصل | Wiring the contact form

النموذج في `contact/index.html` جاهز للربط. افتح الملف وابحث عن:
The form in `contact/index.html` is ready. Open it and find:

```js
const FORM_ENDPOINT = '';
```

أنشئ نموذجاً مجانياً على [formspree.io](https://formspree.io) (أو أي خدمة تقبل `POST` بصيغة JSON)،
ثم ضع رابطه بين علامتي الاقتباس. مثال:
Create a free form at [formspree.io](https://formspree.io) (or any service accepting a JSON `POST`),
then paste its URL between the quotes:

```js
const FORM_ENDPOINT = 'https://formspree.io/f/xxxxxxxx';
```

ما إن يمتلئ هذا المتغير، يُرسل النموذج مباشرة ويعرض رسالة نجاح أو خطأ داخل الصفحة.
وإن بقي فارغاً، يعمل الاحتياط الحالي (فتح برنامج البريد).
Once filled in, the form posts directly and reports success or failure in the page.
Left empty, it falls back to the visitor's mail client.

> النموذج يحوي حقل **honeypot** مخفياً يوقف معظم السبام تلقائياً.
> The form includes a hidden **honeypot** field that stops most spam automatically.

---

## 4. معاينة محلية | Local preview

```bash
python3 -m http.server 8000
```

ثم افتح `http://localhost:8000`. لاحظ أن صفحة 404 لا تظهر محلياً — هذا سلوك Cloudflare Pages فقط.
Then open `http://localhost:8000`. Note the 404 page only appears on Cloudflare Pages, not locally.

---

## 5. لوحة التحكم — ما تغيّر وما يجب معرفته | The CMS: what changed, what to know

### أول استخدام | First run
لم تعد هناك كلمة مرور جاهزة داخل الملف (كانت مكتوبة صراحةً ويمكن لأي زائر قراءتها).
عند فتح اللوحة أول مرة ستُطلب منك كلمة مرور جديدة (٨ محارف فأكثر) وتُحفظ **مُشفّرة (SHA-256)**
على متصفحك وحده. القفل يصمد الآن بعد إعادة تحميل الصفحة، وتُقفل الجلسة تلقائياً بعد ٣٠ دقيقة خمول.

> هذا قفل محلي على المتصفح، لا حماية على الخادم. الحماية الحقيقية أن التوكن موجود على جهازك فقط.

### التوكن | The GitHub token
- يُحفظ في متصفحك بترميز base64 — **ترميز لا تشفير**. أي إضافة متصفح تستطيع قراءته.
- أضِفنا خياراً: **«احفظ التوكن لهذه الجلسة فقط»** (يُمحى عند إغلاق التبويب) — استخدمه على أي جهاز مشترك.
- وزر **«حذف التوكن من هذا الجهاز»**.
- استخدم توكن *Fine-grained* مقصوراً على مستودع الموقع بصلاحية Contents: Read and write فقط.

### ما الذي يغيّر الموقع فعلاً؟ | What actually changes the live site
| الأداة | تكتب في صفحات الموقع؟ |
|---|---|
| مدير صور الصفحات (PIM) | ✅ نعم |
| مدير صور المعرض (GIM) | ✅ نعم |
| محرر الملفات | ✅ نعم |
| الرقع (Patches) | ✅ نعم |
| إصلاح الروابط / Responsive | ✅ نعم |
| زر «النشر» (content.json) | ❌ نسخة احتياطية فقط — لا يقرؤها الموقع |
| المقالات · الاقتباسات · الروابط · مكتبة الصور | ❌ تُحفظ في المتصفح فقط |

الجدول أعلاه صار موضّحاً داخل اللوحة نفسها. **صدّر بياناتك بانتظام** من صفحة «تصدير/استيراد» —
فمسح بيانات الموقع في المتصفح يمحو المقالات والاقتباسات ومكتبة الصور بلا رجعة.

### أعطال أُصلحت | Bugs fixed
- **`initCMS` كان يتوقف بخطأ عند كل دخول** (متغيّر `id` غير معرّف)، فلا تُحمَّل إعدادات GitHub
  ولا حالة الاتصال، وتبقى قائمة **مدير صور المعرض فارغة**. أُصلح — القائمتان تعملان الآن (٩ و٧ صفحات).
- **زر «إصلاح روابط التنقل» كان يحذف روابط**: يستبدل قسم التنقل بنسخة ثابتة من ٤ روابط،
  فيمحو «الرئيسية» و«المعرض» من الصفحات السبع. صار يصحّح **المسارات فقط** دون حذف أي رابط،
  ومعه زر **«فحص بلا تعديل»** يعرض التغييرات قبل تنفيذها.
- **مانيفست صور الصفحة الرئيسية كان قديماً** (١٢ من ١٤ رابطاً غير موجود) فيفشل الاستبدال
  برسالة «الرابط القديم غير موجود». أُعيد بناؤه من الملف الفعلي، وأُضيفت أداة **«فحص صحة مانيفست الصور»**
  تكشف أي تعفّن مستقبلي قبل أن يوقف العمل.
- **صور المعرض المشتركة:** حين تظهر الصورة نفسها في قسمين، الاستبدال يغيّرهما معاً — صار
  المانيفست يُحدَّث للقسمين معاً وتظهر رسالة تخبرك بالأقسام المتأثرة.
- تصحيح نصوص كانت تقول «GitHub Pages» بينما الاستضافة على **Cloudflare Pages**.

### الصفحة الرئيسية | The home page
كانت تحمل ٨ صور حقيقية مضمّنة (٨٥ ك.ب base64) **لا تظهر أبداً** بسبب خطأ في شرط الاستبدال.
استُخرجت إلى ملفات حقيقية في `img/`، وحُذف الكود الميت: حجم `index.html` نزل من **١٥٦ ك.ب إلى ٦٩ ك.ب**
والصور صارت تظهر وقابلة للتخزين المؤقت.

> ⚠️ **مهم:** دقة هذه الصور الثماني منخفضة (٢٨٠ بكسل كحد أقصى) وهي تُستخدم كخلفيات ملء الشاشة،
> فستبدو ناعمة/مشوّشة. أعِد رفعها بدقة عالية من **مدير صور الصفحات (PIM)** — وهو يعمل الآن بعد إصلاح المانيفست.

### ما لم يُنفَّذ | Not done
جعل المقالات والاقتباسات والروابط تُكتب فعلياً في صفحات الموقع. هذا بناء أكبر
(كل صفحة تخزّن محتواها في مصفوفات JS بأسماء مختلفة). حتى ذلك الحين استخدم **محرر الملفات**.
